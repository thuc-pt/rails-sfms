$('#err-date-<%= params[:timesheet_id] %>').html('<%= j t "bookings.new.cant_booking" if @booking.present? || params[:date].to_date == Date.current && @timesheet.start_at.strftime("%H:%M") < Time.now.strftime("%H:%M") || params[:date].blank? || params[:date].to_date < Date.current %>');
