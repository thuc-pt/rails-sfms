<% if @post.valid? %>
  $('#post-<%= @post.id %>').closest('.post-content').hide();
  $('<%= escape_javascript(render @post, team: @post.team_id) %>').prependTo('#posts-area');
  $('#post-form').html('<%= j render "posts/form", post: Post.new, url: team_posts_path(@post.team) %>');
<% end %>
