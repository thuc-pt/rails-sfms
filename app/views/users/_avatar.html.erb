<div class="avatar">
  <%= image_for @user %>
  <%= form_for @user, url: update_avatar_user_path(current_user), html: {id: "update_avatar"} do |f| %>
    <%= f.file_field :image %>
    <label for="user_image"><i class="fa fa-camera"></i></label>
  <% end %>
</div>
